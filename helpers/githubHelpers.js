import axios from 'axios';

export const ghRequest = async ({ url, token, params = {} }) => {

};

// helper to iterate pages (link header)
export async function ghPaginate({ url, token, params = {}, per_page = 100, storeCallback }) {
  
}
